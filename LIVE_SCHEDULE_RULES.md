# LIVE SCHEDULE 運用ルール

## 正本
ライブ予定は `app.js` の `liveEvents` を正本とする。

このファイルは、とら側が別チャット・後日作業でも同じ運用を再現できるようにするための手順書でもある。
ライブ予定を追加・更新する依頼を受けたら、記憶だけで判断せず、必ずこのルールと GitHub `main` 最新版の `app.js` を確認してから作業する。

## 全体の仕組み
ライブ情報は `app.js` の `liveEvents` に1回登録し、そこから以下へ反映する。

1. ファンアプリのホーム「NEXT LIVE」
2. ファンアプリの予定ページ「LIVE CALENDAR」とライブ詳細一覧
3. GitHub Actions 経由の専用 Google Calendar「永遠のセツナ LIVE」

TimeTree は正本にしない。Google Calendar は履歴として過去イベントを残してよい。
アプリ画面と Google Calendar は同じ `liveEvents` を元にする。

## 公開状態
- `status: 'published'`：公開済み。ホーム、予定ページ、Google Calendar 同期の対象。
- `status: 'unreleased'`：内部管理専用。アプリ画面にも Google Calendar にも絶対に出さない。

## 未解禁ライブを新規登録するとき
ユーザーが未公開予定のスクリーンショット、日付、場所、本数などを送ったら、まず GitHub `main` 最新版の `app.js` を取得し、同日・同場所・同本数の `unreleased` がすでに登録されていないか確認する。

ユーザー自身が「前に送ったか」「登録済みか」を覚えている必要はない。重複確認はとら側で行う。

- 既存と同じ予定なら追加しない。
- 同日に本当に追加公演が増えた場合だけ、新しい `unreleased` を追加する。
- 判別できない場合は勝手に重複登録せず、既存データとの違いを確認する。
- タイトル・会場・時間・料金・チケットURLなど、未解禁の情報は推測して埋めない。
- 日付しか分からない場合は日付だけで登録してよい。
- 場所が分かる場合は内部管理用タイトルに場所を使ってよい。
- 複数本ある場合は安定した一意の `id` を分けて付ける。
- `homeUntil` は詳細不明なら当日 `23:59:59+09:00` を暫定値にする。

## 公式詳細が解禁されたとき
ユーザーは基本的に「告知画像 + チケットURL」を送ればよい。
必要に応じて本文・公式投稿など追加情報も使う。

作業手順：
1. GitHub `main` 最新版の `app.js` を取得する。
2. 日付・場所・本数などから、既存の `unreleased` のどれに対応するか照合する。
3. 新しい公開イベントを重複追加するのではなく、該当する既存 `unreleased` を正式情報で更新する。
4. `status` を `published` に変更する。
5. 告知で確認できたタイトル、会場、OPEN/START、出演時間、特典会時間、料金、チケットURL、badge などを入力する。
6. 分からない項目は推測しない。
7. `homeUntil` は特典会終了時刻が分かればその時刻、分からなければ当日 `23:59:59+09:00` を使う。
8. `app.js` 更新後、Google Calendar 同期ワークフローが同じ公開データを同期する。

## NEXT LIVE（ホーム）
ホームは `published` のうち `homeUntil` を過ぎていないイベントだけを対象に、最も近い「2ライブ日」を自動表示する。
同じ日に複数ライブがある場合、その日の公開済みライブをすべて表示した上で、次のライブ日も表示する。
`unreleased` は NEXT LIVE の日付計算にも含めない。

アプリを開いた時・再読み込みした時の現在時刻で判定する。開きっぱなしの画面を分単位で自動再描画する必要はない。いったん閉じて次に開いた時に最新状態へ切り替わればよい。

## 予定ページ：詳細一覧
予定ページのライブ詳細一覧も、`published` のうち `homeUntil` を過ぎていないイベントだけを表示する。
終了したライブは、次にアプリを開く／再読み込みした時に詳細一覧から自動で消える。

## 予定ページ：LIVE CALENDAR
TimeTree の埋め込みではなく、アプリ内の公開・未終了 `liveEvents` から独自カレンダーを描画する。
ライブ日は紫の印で表示し、重要LIVEは強調する。日付をタップすると該当ライブ詳細へ移動する。

月タブは表示対象のライブが存在する月から作る。終了済みライブしかない月は表示対象から外れるため、10月になって9月の表示対象ライブがなくなれば10月が先頭月として自動表示される。

## Google Calendar
専用 Google Calendar「永遠のセツナ LIVE」は `published` のライブだけを同期対象にする。
`unreleased` は絶対に同期しない。

Google Calendar は履歴用途も兼ねるため、ライブ終了後も過去イベントを残す。アプリのホームや予定ページからイベントが消えても、Google Calendar の過去イベントは削除しない。

同期は GitHub Actions と `scripts/sync-google-calendar.mjs` を使う。認証情報は GitHub Actions Secrets に保持し、サービスアカウントJSONや秘密鍵をコード・チャット・公開リポジトリへ貼らない。

## 終了判定
`homeUntil` をアプリ上の終了判定に使う。
特典会終了時刻が分かる場合はその時刻を使う。分からない場合は、その日の `23:59:59+09:00` を暫定値とする。

## 個人予定を混ぜない
ファン全体に関係しない個人的な予定は `liveEvents` に登録しない。
例：ミブのクラウドファンディング特典による 9/21 の個人的なリアル誕生日デートは、公開ライブではないため登録・同期しない。

## 更新時の最重要チェック
1. 作業前に必ず GitHub `main` 最新版 `app.js` を取得する。
2. 未解禁追加時は既存 `unreleased` と照合し、二重登録を防ぐ。
3. 詳細解禁時は既存 `unreleased` を更新し、原則として別イベントを新規追加しない。
4. 未解禁情報をアプリ・Google Calendarへ漏らさない。
5. 情報が不足している項目を推測しない。
6. アプリ表示用の終了処理と Google Calendar の履歴保持を混同しない。
7. ユーザーは未公開予定の登録履歴を覚えている必要はない。GitHub を正本として、とら側が毎回確認する。
