# LIVE SCHEDULE 運用ルール

## 正本
ライブ予定は `app.js` の `liveEvents` を正本とする。

## 公開状態
- `status: 'published'`：ホームの NEXT LIVE と LIVE 一覧に表示する。
- `status: 'unreleased'`：内部データとしてのみ保持し、画面には一切表示しない。

## 未解禁ライブの扱い
TimeTreeなどで日付だけ判明した未解禁予定は、忘れないように `liveEvents` へ先に `unreleased` で登録する。
タイトル・会場・時間・チケットURLなどは、公式解禁前には推測して埋めない。

公式情報が解禁されたら、そのイベントのデータへ正式情報を追記し、`status` を `published` に変更する。

## NEXT LIVE
ホームは `published` のイベントだけを対象に、現在時刻以降の最も近い「2ライブ日」を自動表示する。
同じ日に複数ライブがある場合、その日の公開済みライブをすべて表示した上で、次のライブ日も表示する。
`unreleased` は NEXT LIVE の日付計算にも含めない。

## 終了判定
`homeUntil` は NEXT LIVE 候補から外す時刻。
特典会終了時刻が分かる場合はその時刻を使う。分からない場合は、その日の 23:59:59 を暫定値とする。

## 更新時の注意
作業前に必ず GitHub の `main` 最新版 `app.js` を取得してから編集する。
未解禁情報を公開画面へ漏らさないことを最優先にする。
