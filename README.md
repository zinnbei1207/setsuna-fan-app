# setsuna-fan-app
永遠のセツナ ファン向けWebアプリ / PWA

## LIVE予定の更新ルール（重要）

ライブ情報の正本は `app.js` の `liveEvents`。
今後ライブ予定を追加・解禁するときは、必ず以下のルールで更新する。

### 1. 公式解禁済みライブ
- `status: 'published'` で1公演ずつ登録する。
- 日付、タイトル、会場、OPEN/START、出演・特典会時間、料金、チケットURLなどは、公式告知または主催・出演者側の公開情報で確認できた情報だけを入れる。
- **料金・参加条件もライブ詳細の一部として扱う。** 観覧無料でも、撮影PASS、優先エリア、応援チケット、ドリンク代など別途料金が必要なものが公開されている場合は省略せず併記する。
- 撮影可否や撮影PASSの料金が公開されている場合は、特に「観覧無料」だけを書いて撮影も無料と誤解される表示にしない。
- メンバー欠席、出演変更、販売開始時刻、入場・撮影に関する注意など、来場判断に役立つ公開済みの重要事項も確認できる範囲で追加する。
- 情報が画像・タイムテーブルから確認できる場合も、読める範囲だけを採用し、不鮮明な文字や推測値は確定情報として入れない。
- 推測で詳細を補わない。
- ホーム、LIVE一覧、アプリ内LIVE CALENDARの表示元は `liveEvents` に一本化する。

### 2. 未解禁ライブ
- 予定を把握している場合は、1公演につき1件を `status: 'unreleased'` で登録する。
- 未解禁のイベント名・会場・時間などの内部情報は画面に表示しない。
- ただし、日付とその日の未解禁公演数はファン向けアプリに表示する。
- 表示文言は基本的に以下で統一する。

```
09.17 THU
LIVE
ライブ予定 1本あり
詳細発表をお待ちください
```

- 同じ日に未解禁が複数ある場合、本数は `liveEvents` 内の `unreleased` 件数から自動集計する。手入力で「2本」などを管理しない。
- 例：9/19に `unreleased` が2件なら「ライブ予定 2本あり」と自動表示する。
- 未解禁予定は確定情報ではなく、出演本数の減少・予定変更・中止があり得る。
- 未解禁カードが1件でも表示される場所には、以下の注意文を1回だけ小さく表示する（各カードへ重複表示しない）。

```
※未解禁のライブ予定は変更・中止になる場合があります。最新情報は公式発表をご確認ください。
```

### 3. 同じ日に一部だけ公式解禁された場合
- 解禁された公演だけ、その既存データを `unreleased` → `published` に変更し、公開済みの詳細を追記する。
- 詳細追記時は、日時・会場・出演/特典会だけでなく、料金・撮影条件・欠席情報など公開済みの重要事項も確認する。
- 同日に未解禁公演が残っていれば、公開済み公演は通常カード、残りは「ライブ予定 ○本あり / 詳細発表をお待ちください」として表示する。
- 未解禁本数は残っている `unreleased` だけを自動集計する。

### 4. ホームの NEXT LIVE
- `published` と `unreleased` の両方を日付順の候補として扱う。
- 直近2日分を表示する。
- `published` は通常のライブ詳細カード。
- `unreleased` は通常カードと同じレイアウト感で「ライブ予定 ○本あり / 詳細発表をお待ちください」と表示する。
- 直近2日内に未解禁カードがある場合、カード群の下に未解禁予定の注意文を1回だけ表示する。
- これにより、未解禁予定があるのに「次のライブが数週間後」に見える状態を防ぐ。
- **同じ日に複数の解禁済みライブがある場合は、イベントのSTART時刻（START不明なら出演時刻）を基準に早い順で表示する。** ID文字列や登録順だけで並べない。

### 5. LIVE一覧 / LIVE CALENDAR
- LIVE一覧にも未解禁日を同じルールで表示する。
- LIVE CALENDARにも未解禁日をライブ予定日として表示する。
- 予定ページの注意文は、長いLIVE一覧の最下部ではなく、**LIVE CALENDARのすぐ下**に1回だけ表示する。
- カレンダーから未解禁カードへ移動する場合も、イベント名・会場などの未解禁詳細は出さない。
- LIVE一覧でも、同日複数公演は時間の早い順に表示する。

### 6. 終了判定
- `homeUntil` を過ぎた予定は今後のライブ表示から除外する。
- 未解禁予定は原則その日の `23:59:59+09:00` まで表示対象とする。
- Google Calendar等の履歴を削除するための処理とは分ける。

### 7. 更新時の安全手順（必須）
1. 必ず `zinnbei1207/setsuna-fan-app` の `main` 最新状態を取得する。
2. `README.md` のこのルールを確認する。
3. 最新の `app.js` と対象ファイルを**全文取得**してから編集する。取得結果が途中で切れている・全文を保持できていない場合は `app.js` を更新しない。
4. ライブ1件の追加・変更では、原則として `liveEvents` の該当レコードと並び順に必要な最小箇所以外を変更しない。
5. **`app.js` を更新する前に、更新前の正常な main のコミットSHAを退避ポイントとして確認する。** 異常が出た場合は、その正常コミットへ即時復旧できる状態にしてから書き込む。
6. `app.js` の全文置換が必要な更新では、更新前後でファイル末尾まで保持されていることを確認する。ナビゲーション、曲メニュー、予定画面、LIVE CALENDARなど既存機能のコードを欠落させない。
7. 既存ライブを推測で書き換えたり、別アプリのリポジトリと混同しない。
8. 公式解禁時は、既存の該当 `unreleased` レコードを更新することを優先し、同じ公演を重複登録しない。
9. 公開情報から、日時・会場・出演/特典会・料金・撮影条件・チケット/参加条件・欠席情報など、来場に必要な項目を確認する。
10. 更新後は必ず更新前コミットとの差分を確認し、**依頼箇所以外の大量削除・意図しない変更がないことを確認する。** 想定外の差分があれば公開状態のままにせず復旧する。
11. 更新後、同日公演数・時間順・NEXT LIVE直近2日・LIVE一覧・カレンダー表示の整合性を確認する。
12. 未解禁予定が表示される場合は、注意文がホームではNEXT LIVE直下、予定ページではLIVE CALENDAR直下に正しく表示されていることも確認する。

### 8. 2026-09-15の事故からの固定ルール
- ライブ情報1件の更新時に `app.js` の後半を欠落させ、下部メニュー・曲メニュー・新しい予定表示など既存機能を壊した事故があった。
- 原因は、全文を保持できていない `app.js` を完全なファイルとして上書きしたこと。
- **今後、部分取得・途中で切れた内容を使って `app.js` を丸ごと上書きすることを禁止する。**
- 変更後は「書き込み成功」だけで完了扱いにせず、差分が依頼範囲内であることを確認してから完了とする。

このルールは、2026-09-15時点のFan App仕様として固定する。
